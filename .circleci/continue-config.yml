parameters:
  testfile-build-deploy:
    type: boolean
    default: false

    
workflows:
  testfile-build-deploy-workflow:
    when: << pipeline.parameters.testfile-build-deploy >>
    jobs:
      - run: echo "Hello world3!"
      - build:
          context:
            - my-context
            
jobs:            
  build:
    docker:
      - image: node:current-slim
    steps:
      - run: echo $TEST_ENV_VAR
            
